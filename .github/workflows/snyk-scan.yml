name: Snyk Scan
on:
  push:
    branches: [ main, master, develop, feat/tests-tdd-bdd ] # <-- Corrección 1: Agregada tu rama
  pull_request:
    branches: [ main, master, develop ]
jobs:
  snyk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      - name: Install deps
        run: npm ci
      - name: Run Snyk test
        uses: snyk/actions/node@master # <-- Corrección 2: Usar solo la acción
        with:
          args: test --all-projects --severity-threshold=low
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
